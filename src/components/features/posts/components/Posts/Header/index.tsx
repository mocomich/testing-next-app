import { Typography } from '@/components/ui-elements/Typography'

export const Header = () => {
  return (
    <header>
      <Typography as={'h1'} variant={'h1'}>
        All Posts
      </Typography>
    </header>
  )
}
